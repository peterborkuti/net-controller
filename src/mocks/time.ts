import { DeviceTime } from '../store/model';

export const DEVICETIMES: DeviceTime = {
  100: {allocated: 20, remaining: 0},
  101: {allocated: 20, remaining: 0},
  102: {allocated: 20, remaining: 0}
};
